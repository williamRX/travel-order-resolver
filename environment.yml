name: travel-order-resolver
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pandas>=1.5.0
  - numpy>=1.23.0
  - scikit-learn>=1.2.0
  - matplotlib>=3.6.0
  - seaborn>=0.12.0
  - joblib>=1.2.0
  - tqdm>=4.64.0
  - jupyter>=1.0.0
  - jupyterlab>=3.6.0
  - ipykernel>=6.20.0
  - notebook>=6.5.0
  - pip
  - pip:
    # Packages disponibles uniquement via pip si nécessaire
    # Ajouter ici si besoin

