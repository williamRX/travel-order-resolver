# Core dependencies
pandas>=1.5.0
numpy>=1.23.0

# Machine Learning
scikit-learn>=1.2.0

# Visualization
matplotlib>=3.6.0
seaborn>=0.12.0

# Utilities
joblib>=1.2.0
tqdm>=4.64.0

# Jupyter
jupyter>=1.0.0
jupyterlab>=3.6.0
ipykernel>=6.20.0

# Optional but recommended
notebook>=6.5.0

# NLP
spacy>=3.5.0
seqeval>=1.2.2  # Métriques NER (precision, recall, F1 pour les entités)

# Transformers / Deep Learning (pour CamemBERT)
transformers>=4.30.0
torch>=2.0.0  # Pour Mac Apple Silicon: pip install torch torchvision torchaudio (support MPS inclus)
accelerate>=0.20.0
sentencepiece>=0.1.99  # Requis pour CamemBERT tokenizer
protobuf>=4.25.0       # Requis par SentencePieceExtractor (transformers + sentencepiece)
# Optionnel mais recommandé : évite les erreurs de fallback tokenizer récents
tiktoken>=0.5.0

# Note pour Mac Apple Silicon (M1/M2/M3/M4):
# PyTorch avec support MPS (Metal Performance Shaders) est installé automatiquement
# avec: pip install torch torchvision torchaudio
# Voir docs/MPS_GPU_SETUP.md pour plus de détails

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6  # Requis pour les uploads de fichiers (File/UploadFile)
requests>=2.31.0  # Requis pour l'API SNCF (appels HTTP)
